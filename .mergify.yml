pull_request_rules:
  - name: auto-merge when Codex or Copilot CI passes
    conditions:
      - -draft
      - "check-success=codex OR check-success=copilot"
    actions:
      merge:
        method: squash
